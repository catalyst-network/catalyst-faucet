{
  "name": "catalyst-faucet-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Catalyst testnet faucet backend (Fastify + ethers + Redis + Postgres).",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "prisma:generate": "prisma generate",
    "prisma:migrate:dev": "prisma migrate dev",
    "prisma:migrate:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@fastify/helmet": "^13.0.1",
    "@fastify/sensible": "^6.0.3",
    "@prisma/client": "^6.0.0",
    "dotenv": "^17.2.1",
    "ethers": "^6.15.0",
    "fastify": "^5.4.0",
    "ioredis": "^5.7.0",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.36.0",
    "@types/node": "^24.3.0",
    "@vitest/coverage-v8": "^3.2.0",
    "eslint": "^9.36.0",
    "prettier": "^3.6.2",
    "prisma": "^6.0.0",
    "ioredis-mock": "^8.12.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.44.0",
    "vitest": "^3.2.0"
  }
}
